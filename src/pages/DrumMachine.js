import DMBuilder from "../components/DMBuilder";
import '../styles/drumMachine.scss';

const DrumMachine = () => {
    return (
        <div id='drum-machine'>
            <DMBuilder />
        </div>
    );
}

export default DrumMachine;