import DMBuilder from "../components/DMBuilder";

const DrumMachine = () => {
    return (
        <div id='drum-machine'>
            <DMBuilder />
        </div>
    );
}

export default DrumMachine;